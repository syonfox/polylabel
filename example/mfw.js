class MangoLeafMap {

    constructor() {

        this.requestGeolocation(0);
        this.requestPhoto(0);

    }

}
                                
/*

we are creating a map of hex codes around the globe

each photo is taged with a h3code

then users can brows a view of the photos in any h3code

Features
   - upload pgoto to a h3 cell.


   - heatmap
   - prf
        - usr, loc, dsc


    - prf(uid, usr, eml, img, loc, dsc) //

    - user = usr(id, eml, pwd, usr, cll, dsc)
    - post = pst(id, uid, cll, img, dsc)
    - //link = lnk(id, pid, uid)
    - //react= rct(...)

    - post(id, user_id, loc, img, desc)
    - user(id, name,  hash, email, loc, photo)

    - pst(d, sr_d, lc, mg, dsc)
    - sr(d, nm,  hsh, ml, lc, pht)
    - //dt(d, nm, mt, dt, tm)
    - //dt(id, name, meta, data, time)
 */